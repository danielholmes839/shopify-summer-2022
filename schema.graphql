type Product {
    id: ID!
    name: String!
    description: String!
    price: Float!
    category: String
}

# output for product mutations
type ProductPayload {
    error: String       # error message
    product: Product
}

# input for product mutations
input ProductInput {
    name: String!
    description: String!
    price: Float!
    category: String
}


type Mutation {
    productCreate(input: ProductInput!): ProductPayload!
    productUpdate(productId: ID!, input: ProductInput!): ProductPayload!       # product update
    productUpdateCategory(productId: ID!, category: String): ProductPayload!   # product update specifically for categories
    productDelete(productId: ID!): ProductPayload!
}

type Query {
    product(productId: ID!): Product
    products: [Product!]!
    productsByCategory(category: String): [Product!]!
}
